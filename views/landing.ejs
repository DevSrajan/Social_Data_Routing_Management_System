<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome | Post App</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="icon" href="favicon.ico" type="image/x-icon">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <!-- Custom CSS -->
  <style>
    :root {
      --primary-color: #6a5acd; /* Main background color */
      --accent-color: #00ffab; /* Accent for headings */
      --text-color: #d5ffff;   /* Light text color */
      --button-primary: #ff0000; /* Primary button color */
      --button-primary-hover: #228b22; /* Hover state */
    }
    body {
      font-family: 'Poppins', sans-serif;
      background-color: var(--primary-color);
      color: var(--text-color);
      margin: 0;
      padding: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    .landing-container {
      text-align: center;
      padding: 40px 20px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
      max-width: 600px;
    }
    h1 {
      color: var(--accent-color);
      font-size: 3rem;
      margin-bottom: 20px;
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    p {
      font-size: 1.2rem;
      margin-bottom: 30px;
      line-height: 1.5;
    }
    .btn-primary {
      padding: 15px 30px;
      font-size: 1.3rem;
      font-weight: bold;
      background-color: var(--button-primary);
      color: white;
      border: none;
      border-radius: 10px;
      transition: all 0.3s ease;
      text-transform: uppercase;
      text-decoration: none;
    }
    .btn-primary:hover {
      background-color: var(--button-primary-hover);
      transform: translateY(-3px);
    }
    @media (max-width: 768px) {
      h1 {
        font-size: 2.5rem;
      }
      p {
        font-size: 1rem;
      }
      .btn-primary {
        font-size: 1.1rem;
        padding: 12px 25px;
      }
    }
  </style>
</head>
<body>
  <div class="landing-container">
    <h1><b>Fetching & CRUD Management</b></h1>
    <p>Effortlessly manage your posts. Create, Read, Update, Delete (CRUD) with an intuitive interface.</p>
    <a href="/posts" class="btn btn-primary">Go to Posts</a>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
